sudo docker run --rm -v $(realpath .)/:/mnt arnx/nautilus-slave
mkdir -p .iso/boot/grub && cp configs/grub.cfg .iso/boot/grub && cp nautilus.bin nautilus.syms .iso/boot && grub-mkrescue -o nautilus.iso .iso
qemu-system-x86_64 -s -serial stdio -vga std -smp 1 -m 2048 -cdrom nautilus.iso
